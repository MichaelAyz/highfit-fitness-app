# Server Configuration
PORT=4000

# Optional Database Connection
# If unset, the backend automatically uses resilient local file-backed storage (src/db/local_db.json)
DATABASE_URL=postgresql://postgres:password@localhost:5432/aifitness

# Optional Google Gemini API Key
# If unset, the engine runs fully offline using deterministic algorithmic solver
GEMINI_API_KEY=
